# Mobile Shop ExpressJS MVC

Dự án được sinh tự động theo mô hình: config / constants / middlewares / models / services / controllers / routes / views / public.

## Cách chạy
1. npm install
2. sao chép .env.example thành .env và chỉnh lại cấu hình
3. npm run dev
